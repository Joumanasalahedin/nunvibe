services:
  nunvibe-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nunvibe-backend
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    restart: unless-stopped
